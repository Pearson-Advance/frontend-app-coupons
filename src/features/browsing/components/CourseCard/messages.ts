import { defineMessages } from 'react-intl';

const messages = defineMessages({
  enroll: {
    id: 'browsing.components.courseCard.enrollButton',
    defaultMessage: 'Enroll Now',
  },
});

export default messages;
